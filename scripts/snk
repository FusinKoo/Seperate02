#!/usr/bin/env bash
set -euo pipefail
# 始终用 runner 环境调用 snakemake，避免 "command not found"
if ! command -v micromamba >/dev/null 2>&1; then
  echo "[ERR] micromamba not found. Run: make env && make setup" >&2
  exit 2
fi
exec micromamba run -n runner snakemake "$@"
